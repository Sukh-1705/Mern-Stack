# Mern-Stack
Mearn Stack Repo Learning
1..10 | ForEach-Object { $folder = "day_{0:D2}" -f $_; mkdir $folder; "## $folder" | Out-File "$folder/README.md" }

git add .  will stage your changes
git commit save changes
git push origin maincd..